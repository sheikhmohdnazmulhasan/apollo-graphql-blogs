import { Query } from "../queries";
import { Mutation } from "../mutations";

// This is the resolver for all the queries and mutations that we have defined in the queries and mutations files
export const resolvers = {
  Query,
  Mutation,
};
